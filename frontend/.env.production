VITE_DEFAULT_LOGIN_EMAIL=admin@connoisseurauto.co.za
VITE_SHOW_DEMO_CREDENTIALS=false

# Full Supabase cutover, 2026-08-13 (explicit user instruction: "get every single thing off
# firebase... do the cutover now"). VITE_SUPABASE_ANON_KEY is the publishable/anon key --
# safe to commit, designed to be public, constrained by RLS (supabase/migrations/*.sql).
# Never put the secret/service_role key here. Firebase's config vars (VITE_FIREBASE_*) were
# removed from this file -- frontend/src/lib/firebase.js (the only file that ever read them)
# was deleted, so nothing in the frontend reads them anymore.
VITE_SUPABASE_URL=https://cjvrquipmnoihksijful.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_Eu9Qx2QyjMZVLhKcv8232A_KvxjTbbi
VITE_AUTH_BACKEND=supabase
