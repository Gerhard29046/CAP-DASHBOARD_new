VITE_DEFAULT_LOGIN_EMAIL=admin@connoisseurauto.co.za
VITE_SHOW_DEMO_CREDENTIALS=false

# Supabase -- the only backend this app uses (full cutover, 2026-08-13). Dashboard notes
# (sticky notes) use Supabase Auth + RLS directly like every other table -- no separate API
# base URL/backend service needed (see supabase/migrations/0023_dashboard_notes_direct_rls.sql).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_AUTH_BACKEND=supabase
