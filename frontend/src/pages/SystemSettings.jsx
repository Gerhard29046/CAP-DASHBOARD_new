import{useEffect,useState}from"react";import{apiClient}from"@/api/apiClient";import{useAuth}from"@/lib/AuthContext";import{Button}from"@/components/ui/button";import{Checkbox}from"@/components/ui/checkbox";
export default function SystemSettings(){const{hasPermission}=useAuth();const[status,setStatus]=useState(null),[calendars,setCalendars]=useState([]),[message,setMessage]=useState("");const load=async()=>{const s=await apiClient.request('/google-calendar/status');setStatus(s);if(s.connected)setCalendars(await apiClient.request('/google-calendar/calendars'))};useEffect(()=>{load().catch(e=>setMessage(e.message))},[]);const connect=async()=>{const r=await apiClient.request('/google-calendar/connect');window.location.assign(r.authorization_url)};const save=async()=>{await apiClient.request('/google-calendar/calendars',{method:'PUT',body:JSON.stringify({calendar_ids:calendars.filter(c=>c.selected).map(c=>c.id)})});setMessage('Selected calendars saved.');load()};const disconnect=async()=>{await apiClient.request('/google-calendar/disconnect',{method:'DELETE'});setCalendars([]);load()};return <div className="max-w-3xl space-y-5"><div><h1 className="text-2xl font-bold">System Settings</h1><p className="text-muted-foreground">Manage secure external integrations.</p></div>{message&&<div className="rounded-xl border p-3 text-sm">{message}</div>}<section className="rounded-2xl border bg-card p-6 space-y-4"><h2 className="text-lg font-bold">Google Calendar</h2>{!status?<p>Loading connection status…</p>:!status.connected?<><p>Google Calendar is not connected.</p><p className="text-sm text-muted-foreground">Connect the authorised company Google account to display selected calendar events in the CAP Dashboard. The integration is read-only and will not change the Google Calendar.</p>{hasPermission('calendar.google.connect')&&<Button onClick={connect}>Connect Google Calendar</Button>}</>:<><dl className="grid grid-cols-[160px_1fr] gap-2 text-sm"><dt>Connection Status</dt><dd>Connected</dd><dt>Account</dt><dd>{status.account_name||'—'} ({status.account_email})</dd><dt>Connection Date</dt><dd>{status.connected_at||'—'}</dd><dt>Last Refresh</dt><dd>{status.last_refreshed_at||'Not refreshed yet'}</dd></dl>{status.requires_reconnection&&<p className="text-destructive">Google Calendar must be reconnected.</p>}<div className="space-y-2">{calendars.map((c,i)=><label className="flex items-center gap-2" key={c.id}><Checkbox checked={c.selected} onCheckedChange={v=>setCalendars(calendars.map((x,n)=>n===i?{...x,selected:!!v}:x))}/>{c.name}{c.primary?' (Primary)':''}</label>)}</div><div className="flex flex-wrap gap-2">{hasPermission('calendar.google.calendars.select')&&<Button onClick={save}>Change Selected Calendars</Button>}{hasPermission('calendar.google.connect')&&<Button variant="outline" onClick={connect}>Replace Connection</Button>}{hasPermission('calendar.google.disconnect')&&<Button variant="destructive" onClick={disconnect}>Disconnect</Button>}</div></>}</section></div>}
