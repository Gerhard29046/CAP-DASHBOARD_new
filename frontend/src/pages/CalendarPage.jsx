import { useCallback, useEffect, useRef, useState } from "react";
import FullCalendar from "@fullcalendar/react";import dayGridPlugin from "@fullcalendar/daygrid";import timeGridPlugin from "@fullcalendar/timegrid";import listPlugin from "@fullcalendar/list";import interactionPlugin from "@fullcalendar/interaction";
import { RefreshCw, X } from "lucide-react";import { apiClient } from "@/api/apiClient";import { useAuth } from "@/lib/AuthContext";import { Button } from "@/components/ui/button";import { Switch } from "@/components/ui/switch";import { Input } from "@/components/ui/input";
export default function CalendarPage(){const{hasPermission}=useAuth();const ref=useRef(null);const[events,setEvents]=useState([]),[loading,setLoading]=useState(false),[error,setError]=useState(""),[warnings,setWarnings]=useState([]),[range,setRange]=useState(null),[services,setServices]=useState(true),[google,setGoogle]=useState(hasPermission("calendar.google.view")),[selected,setSelected]=useState(null);const load=useCallback(async()=>{if(!range)return;setLoading(true);setError("");try{const q=new URLSearchParams({start:range.startStr,end:range.endStr,include_services:services?"1":"0",include_google:google?"1":"0"});const data=await apiClient.request(`/calendar/events?${q}`);setEvents(data.events||[]);setWarnings(data.warnings||[])}catch(e){setError(e.message);setEvents([])}finally{setLoading(false)}},[range,services,google]);useEffect(()=>{load()},[load]);const eventClass=e=>e.event.extendedProps.sourceType==="google_calendar"?["calendar-google"]:e.event.extendedProps.status?.toLowerCase()==="completed"?["calendar-completed"]:new Date(e.event.start)<new Date().setHours(0,0,0,0)?["calendar-overdue"]:["calendar-service"];return <div className="space-y-4 overflow-hidden"><div className="flex flex-wrap items-center justify-between gap-3"><div><h1 className="text-2xl font-bold">Calendar</h1><p className="text-sm text-muted-foreground">Upcoming Services and read-only Google Calendar events.</p></div><Button variant="outline" onClick={load} disabled={loading}><RefreshCw className={`w-4 h-4 mr-2 ${loading?"animate-spin":""}`}/>Refresh Calendar</Button></div><div className="flex flex-wrap gap-5 rounded-xl border bg-card p-3"><label className="flex items-center gap-2 text-sm"><Switch checked={services} onCheckedChange={setServices}/>Show Upcoming Services</label>{hasPermission("calendar.google.view")&&<label className="flex items-center gap-2 text-sm"><Switch checked={google} onCheckedChange={setGoogle}/>Show Google Calendar</label>}</div>{error&&<div className="rounded-xl border border-destructive/40 bg-destructive/10 p-3 text-sm">{error}</div>}{warnings.map((w)=><div key={w} className="rounded-xl border bg-muted p-3 text-sm">{w}</div>)}<div className="relative min-h-[650px] rounded-2xl border bg-card p-3 md:p-5">{loading&&<div className="absolute right-5 top-5 z-10 rounded-lg bg-background/90 px-3 py-2 text-sm">Loading calendar…</div>}<FullCalendar ref={ref} plugins={[dayGridPlugin,timeGridPlugin,listPlugin,interactionPlugin]} initialView={window.innerWidth<640?"listMonth":"dayGridMonth"} headerToolbar={{left:"prev,next today",center:"title",right:"dayGridMonth,timeGridWeek,timeGridDay,listMonth"}} buttonText={{month:"Month",week:"Week",day:"Day",list:"Agenda"}} timeZone="Africa/Johannesburg" height="auto" events={events} editable={false} selectable={false} datesSet={setRange} eventClick={({event})=>setSelected(event)} eventClassNames={eventClass} noEventsContent="No calendar events in this date range."/></div>{selected&&<EventDetails event={selected} canReschedule={hasPermission("upcoming_services.update")} close={()=>setSelected(null)} refreshed={()=>{setSelected(null);load()}}/>}</div>}
function EventDetails({event,canReschedule,close,refreshed}){const p=event.extendedProps;const[date,setDate]=useState(event.startStr?.slice(0,10)||"");const[busy,setBusy]=useState(false);const google=p.sourceType==="google_calendar";const reschedule=async()=>{setBusy(true);try{await apiClient.request(`/service-records/${p.serviceRecordId}`,{method:"PATCH",body:JSON.stringify({next_service_due:date})});refreshed()}finally{setBusy(false)}};return <div className="fixed inset-0 z-[100] flex items-center justify-center bg-black/70 p-4" onClick={close}><div className="w-full max-w-lg rounded-2xl border bg-card p-6 shadow-2xl" onClick={e=>e.stopPropagation()}><div className="flex justify-between gap-3"><h2 className="text-xl font-bold">{event.title}</h2><button onClick={close}><X/></button></div><dl className="mt-5 grid grid-cols-[130px_1fr] gap-2 text-sm">{google?<><dt>Start</dt><dd>{event.start?.toLocaleString()}</dd><dt>End</dt><dd>{event.end?.toLocaleString()}</dd><dt>Calendar</dt><dd>{p.calendarName}</dd><dt>Location</dt><dd>{p.location||"—"}</dd><dt>Organiser</dt><dd>{p.organiser||"—"}</dd><dt>Description</dt><dd className="whitespace-pre-wrap">{p.description||"—"}</dd></>:<><dt>Client</dt><dd>{p.clientName}</dd><dt>Machine</dt><dd>{[p.machineBrand,p.machineModel].filter(Boolean).join(" ")}</dd><dt>Serial Number</dt><dd>{p.serialNumber||"—"}</dd><dt>Refrigerant</dt><dd>{p.refrigerantType||"—"}</dd><dt>Technician</dt><dd>{p.technician||"—"}</dd><dt>Status</dt><dd>{p.status}</dd><dt>Notes</dt><dd>{p.notes||"—"}</dd></>}</dl><div className="mt-5 flex flex-wrap gap-2">{!google&&<><Button asChild variant="outline"><a href={`/clients/${p.clientId}`}>View Client</a></Button><Button asChild variant="outline"><a href={`/machines/${p.machineId}`}>View Machine</a></Button><Button asChild variant="outline"><a href={`/service-records?record=${p.serviceRecordId}`}>View Service Record</a></Button></>}{google&&p.htmlLink&&<Button asChild><a href={p.htmlLink} target="_blank" rel="noreferrer">Open in Google Calendar</a></Button>}</div>{!google&&canReschedule&&<div className="mt-5 flex gap-2"><Input type="date" value={date} onChange={e=>setDate(e.target.value)}/><Button onClick={reschedule} disabled={busy||!date}>Reschedule Service</Button></div>}</div></div>}
