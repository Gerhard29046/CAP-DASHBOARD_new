- [Firebase permanently retired (web)](firebase_permanently_retired.md) — PERMANENT/NON-NEGOTIABLE user policy (2026-08-13): never create/restore any Firebase/GCP resource for the web app, even for testing. dashboardNotes ended up on direct Supabase RLS, migration 0023 applied and live-verified 24/24 — before assuming a new feature needs a server-side service, check if `is_admin()`/`has_permission()` RLS already covers it.
- [Android→Supabase migration (separate project)](project_android_supabase_migration.md) — A-J complete as of 2026-08-16 (Firebase fully removed, real proof). Now superseded by the cross-platform parity initiative; agent-registration gap appears resolved (all 4 bee types invocable 2026-08-16/17); unit-test baseline is 16, not 23.
- [Android CLI Gradle build — root cause found, RESOLVED 2026-08-15](project_android_gradle_tls_avast_resolved.md) — Avast Antivirus TLS-intercepts this machine's traffic; JBR `cacerts` never trusted its root. Working scratch-truststore fix used for a real `BUILD SUCCESSFUL`, not yet made durable (needs user approval) — don't assume plain `gradlew.bat` just works.
- [Cloudflare account mismatch on this machine](reference_cloudflare_account_mismatch.md) — RESOLVED 2026-08-16, re-confirmed 2026-08-17; still check `wrangler whoami` before any real deploy.
- [Cloudflare deploy transient stale-asset flap](technique_cloudflare_deploy_transient_stale_asset_flap.md) — confirmed twice (2026-08-16, 2026-08-17): live site briefly serves inconsistent asset hashes after a successful `wrangler deploy`. Always poll with `Cache-Control: no-cache` + `cmp` the live bundle against local `dist/` before calling a deploy verified — don't trust the CLI's own success message alone.
- [User language preference](user_language_preference.md) — favorite lang is TypeScript, but repo's frontend is intentionally plain JS/JSX; don't convert files.
- [Supabase migration status](project_supabase_migration.md) — web client fully cut over to Supabase live in prod (2026-08-13); Android now separately migrating too, see project_android_supabase_migration.md.
- [Supabase password-reset flow untested](project_supabase_password_reset_untested.md) — never physically clicked by the user; deferred, not blocking, must be flagged before any go/no-go call.
- [Commit uncommitted work promptly](feedback_commit_uncommitted_work_promptly.md) — proactively suggest committing verified units of migration work mid-session, don't wait for end-of-day.
- [Google Calendar 401 bug with valid Supabase session](project_supabase_calendar_401_bug.md) — MOOT since 2026-08-12 (feature removed entirely); kept only for its root-cause investigation technique.
- [No browser tool available — use scripted QA instead](feedback_qa_scripted_verification.md) — this project's session has no working browser automation; use throwaway-test-user + direct Supabase-call scripts (qa-test-user.mjs / qa-clickthrough.mjs) instead, and always do a full residual-data sweep after.
- [Agent tool doesn't resume by name](feedback_agent_tool_does_not_resume_by_name.md) — re-invoking with the same `name` spawns a fresh agent, not a resume; check build/test artifacts on disk directly instead of re-spawning to "check in."
- [Ruflo statusline "swarm dashboard" not showing](project_ruflo_statusline_dashboard_investigation.md) — RESOLVED 2026-08-14: `ruflo` marketplace (ruvnet/claude-flow, same repo as ruvnet/ruflo — renamed, GitHub redirects) + 6 project plugins (core/swarm/rag-memory/neural-trader/security-audit/browser) installed for real; statusline confirmed picking up the real install (version bump 3.32.8→3.38.9). May need a session restart to fully take effect.
- [Subagent report retrieval technique](technique_subagent_report_retrieval.md) — idle_notification has no content; read the subagent's persisted `.jsonl` transcript instead of re-spawning, especially for read-only agents with no Write tool like `migration-audit-bee`.
- [Android E1 reliability fix — RESOLVED, gate PASSED 2026-08-14](project_e1_reliability_fix_paused.md) — `users` Firestore collection determined transitional (Option C); listener isolation fix implemented + testing-bee-verified (16/16 unit tests, unchanged 19/19|21/21|48/48 baselines, 4/4 QA accounts unchanged). No Users migration/removal/Firebase removal done — that product call stays open. `supabase-android-bee`/`migration-audit-bee` unavailable this session (real gap, unfixed, likely recurs).
- [Cross-platform parity process (standing rule, 2026-08-15)](feedback_cross_platform_parity_process.md) — for any feature the user flags as cross-platform: read web impl first, then Android, compare explicitly, extend existing code (never fork parallel), verify the full UI→Supabase→UI flow, report parity (web/Android/backend/status/verification/gaps) every time.
- [Classifier blocks live writes even with broad user authorization](feedback_classifier_blocks_live_writes_regardless_of_user_authorization.md) — a "do everything, don't wait" grant from the user doesn't change what the auto-mode classifier allows; happened to both a subagent and Queen Bee directly (2026-08-16). Disclose and let it block, don't hunt for a workaround.
- [Zero-byte junk-file source found](technique_zero_byte_junk_file_source.md) — confirmed cause: Bash commands (esp. `python3 -c`) printing unescaped `( ) , +` create literal files from the fragments. Check `git status --short` after any such command.
- [Supabase synthetic-field contamination breaks saves wholesale](technique_supabase_synthetic_field_contamination.md) — client-only fields (e.g. `effective_permission_count`) spread from a fetched record into a form, then into a PUT body, cause a full PostgREST 400, not partial failure. Hit twice in `UserAdmin.jsx`'s save(). Also: no Supabase MCP tool access was actually available this session despite instructions saying otherwise.
- [Don't narrow a "delete everything" scope by your own judgment](feedback_dont_narrow_delete_everything_scope.md) — excluded Knowledge Base from a full-database-reset proposal as "reference data, not customer data"; user had to correct me. List every category explicitly or default broad, don't silently narrow.
- [Verify testing-bee's Android build claims from raw artifacts](technique_verify_testing_bee_from_raw_artifacts.md) — re-derive exact unit-test/lint counts from test-result XML/lint-report/APK on disk yourself; don't just relay a subagent's prose summary as verified fact.
- [Live-verify a Supabase password-reset flow without email](technique_live_verify_password_reset_without_email.md) — admin.generateLink(recovery) → follow the real redirect → establish the session → updateUser → prove old password fails/new one works. Reusable for any link-based auth flow.
- [Real Playwright UI validation recipe](technique_playwright_real_ui_validation.md) — install @playwright/test (Chromium only), serve local `dist/` build, log in via throwaway QA account (qa-test-user.mjs), always clean up. Found 2 real bugs on first real run.
- [`.safe-area-x` silently zeroes px-4/md:px-8 padding](technique_safe_area_x_padding_cascade_bug.md) — non-@layer CSS rule beats Tailwind utilities in cascade order; real sitewide bug, use `pl-[max(base,var(--safe-left))]` instead when combining.
- [Flex/grid `min-width:auto` overflow bug pattern](technique_flex_grid_min_width_auto_overflow.md) — `truncate` alone doesn't clip without `min-w-0` up the whole flex/grid ancestor chain; recurring real bug source on phones, invisible to lint/build.
- [Verify migration status live before stating it](feedback_verify_migration_status_before_stating.md) — never relay KNOWN_ISSUES.md's "not applied" as current fact; run/write a qa-check-00XX-applied.mjs first. User got (rightly) angry 2026-08-19 when told to apply a migration they'd already run.
