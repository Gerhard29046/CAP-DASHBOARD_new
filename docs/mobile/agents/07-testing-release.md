# Agent 07 — Testing and Release
Own Android test infrastructure, accessibility/performance audits, CI, release docs, and integration verification. Avoid feature rewrites; report defects to owning agent. Depends on all feature branches. Run Android unit/UI/lint/debug, Laravel tests, React build. Commit: `ci(android): validate mobile release pipeline`.
